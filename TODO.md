# Garlic Claw TODO

## 当前边界

- 这轮只处理“记忆”。
- 目标是把记忆恢复为纯插件实现：
  - `builtin.memory-tools`
  - `builtin.memory-context`
- 不扩到 `conversation-title / context-compaction / runtime-tools / subagent / plugin-pc`。
- `shared` 只放类型，不放逻辑。
- 不新增 `helper / helpers` 命名。
- 禁止 `any`。

## 当前阶段

- 当前无进行中阶段。
